# 🤖 Qwen API Client (Unofficial)

Этот скрипт позволяет взаимодействовать с **Tongyi Qwen** — мощной языковой моделью от Alibaba Cloud — через неофициальный API.  
Скрипт отправляет запрос к серверу Qwen и получает ответ в режиме потоковой передачи (streaming), поддерживая как основной текст, так и «мышление» модели (`thinking`).

> ⚠️ **Важно**: Этот проект использует гостевые (guest) cookies и может перестать работать в любой момент, если Alibaba изменит свою систему аутентификации или закроет публичный доступ.

---

## 🔧 Возможности

- Отправка пользовательских сообщений в Qwen.
- Поддержка различных моделей: `qwen3-max`, `qwen3-coder-flash` и др.
- Парсинг как финального ответа (`text`), так и внутреннего «мышления» модели (`think`).
- Красивый цветной вывод в терминале 🎨.

---

## 📦 Требования

- Python 3.7+
- Библиотеки:
  - `requests`
  - `uuid` (встроена в Python)

Установите зависимости:

```bash
pip install requests
```

---

## ▶️ Запуск

1. Сохраните код в файл, например `qwen_client.py`.
2. Запустите:

```bash
python qwen_client.py
```

Скрипт автоматически отправит тестовый запрос:  
> *«Кто ты? Какая модель? Какая дата?»*

И выведет ответ от модели.

---

## 🛠 Как изменить модель?

В коде найдите строку:

```python
models = "tongyi-qwen3-coder-flash"
```

Замените значение на одну из поддерживаемых моделей:

| Модель | Описание |
|--------|--------|
| `tongyi-qwen3-max` | Максимальная версия Qwen 3 |
| `tongyi-qwen3-max-thinking` | Версия с поддержкой цепочек рассуждений |
| `tongyi-qwen-plus-latest` | Улучшенная средняя модель |
| `tongyi-qwen3-coder` | Специализированная для программирования |
| `tongyi-qwen3-coder-flash` | Быстрая и легкая версия для кода |

> 💡 Совет: вы можете легко модифицировать функцию `qianwen()` для передачи собственных сообщений.

---

## ⚠️ Предупреждение

- Использование этого скрипта **нарушает условия использования** официального сервиса Tongyi Qwen.
- Cookies жёстко закодированы и могут стать недействительными в любой момент.
- Не используйте в коммерческих целях.
- Автор не несёт ответственности за возможные последствия.

---

## 👨‍💻 Автор

- **EndermanHack19**
- GitHub: [https://github.com/AniLand-company](https://github.com/AniLand-company)
- Поддержать автора: [Donate ❤️](https://clck.ru/3F2nHg)

---

## 📜 Лицензия

Этот проект распространяется **без лицензии**. Используйте на свой страх и риск.

--- 

> ✨ *Сделано с любовью и любопытством.*
